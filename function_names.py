import numpy as np
from random import *
import math
def real_fun(functions):

    functions["Traj1"] = lambda xt: np.asarray([xt[0][0],xt[0][1],xt[0][2]+ 0.25])
    functions["Traj2"] = lambda xt: np.asarray([xt[0][0], xt[0][1], xt[0][2]-0.25])
    functions["Traj3"] = lambda xt: np.asarray([xt[0][0], xt[0][1]+ 0.25, xt[0][2]])
    functions["Traj4"] = lambda xt: np.asarray([xt[0][0], xt[0][1]-0.25, xt[0][2]])
    functions["Traj5"] = lambda xt: np.asarray([xt[0][0]+0.25, xt[0][1], xt[0][2]])
    functions["Traj6"] = lambda xt: np.asarray([xt[0][0]-0.25, xt[0][1], xt[0][2]])
    functions["Traj7"] = lambda xt: np.asarray([xt[0][0]+0.12, xt[0][1]+0.12, xt[0][2]])
    functions["Traj8"] = lambda xt: np.asarray([xt[0][0]+0.12, xt[0][1]-0.12, xt[0][2]])
    functions["Traj9"] = lambda xt: np.asarray([xt[0][0]-0.12, xt[0][1]+0.12, xt[0][2]])
    functions["Traj10"] = lambda xt: np.asarray([xt[0][0]-0.12, xt[0][1]-0.12, xt[0][2]])
    functions["Traj11"] = lambda xt: np.asarray([xt[0][0], xt[0][1] + 0.25, xt[0][2]+0.12])
    functions["Traj12"] = lambda xt: np.asarray([xt[0][0], xt[0][1] - 0.25, xt[0][2]+0.12])
    functions["Traj13"] = lambda xt: np.asarray([xt[0][0] + 0.25, xt[0][1], xt[0][2]+0.12])
    functions["Traj14"] = lambda xt: np.asarray([xt[0][0] - 0.25, xt[0][1], xt[0][2]+0.12])
    functions["Traj15"] = lambda xt: np.asarray([xt[0][0] + 0.12, xt[0][1] + 0.12, xt[0][2]+0.12])
    functions["Traj16"] = lambda xt: np.asarray([xt[0][0] + 0.12, xt[0][1] - 0.12, xt[0][2]+0.12])
    functions["Traj17"] = lambda xt: np.asarray([xt[0][0] - 0.12, xt[0][1] + 0.12, xt[0][2]+0.12])
    functions["Traj18"] = lambda xt: np.asarray([xt[0][0] - 0.12, xt[0][1] - 0.12, xt[0][2]+0.12])
    functions["Traj10"] = lambda xt: np.asarray([xt[0][0], xt[0][1] + 0.25, xt[0][2] - 0.12])
    functions["Traj20"] = lambda xt: np.asarray([xt[0][0], xt[0][1] - 0.25, xt[0][2] - 0.12])
    functions["Traj21"] = lambda xt: np.asarray([xt[0][0] + 0.25, xt[0][1], xt[0][2] - 0.12])
    functions["Traj22"] = lambda xt: np.asarray([xt[0][0] - 0.25, xt[0][1], xt[0][2] - 0.12])
    functions["Traj23"] = lambda xt: np.asarray([xt[0][0] + 0.12, xt[0][1] + 0.12, xt[0][2] - 0.12])
    functions["Traj24"] = lambda xt: np.asarray([xt[0][0] + 0.12, xt[0][1] - 0.12, xt[0][2] - 0.12])
    functions["Traj25"] = lambda xt: np.asarray([xt[0][0] - 0.12, xt[0][1] + 0.12, xt[0][2] - 0.12])
    functions["Traj26"] = lambda xt: np.asarray([xt[0][0] - 0.12, xt[0][1] - 0.12, xt[0][2] - 0.12])
    functions["Traj27"] = lambda xt: np.asarray([xt[0][0], xt[0][1] + 0.25, xt[0][2] + 0.25])
    functions["Traj28"] = lambda xt: np.asarray([xt[0][0], xt[0][1] - 0.25, xt[0][2] + 0.25])
    functions["Traj29"] = lambda xt: np.asarray([xt[0][0] + 0.25, xt[0][1], xt[0][2] + 0.25])
    functions["Traj30"] = lambda xt: np.asarray([xt[0][0] - 0.25, xt[0][1], xt[0][2] + 0.25])
    functions["Traj31"] = lambda xt: np.asarray([xt[0][0], xt[0][1] + 0.12, xt[0][2] + 0.25])
    functions["Traj32"] = lambda xt: np.asarray([xt[0][0], xt[0][1] - 0.12, xt[0][2] + 0.25])
    functions["Traj33"] = lambda xt: np.asarray([xt[0][0] + 0.12, xt[0][1], xt[0][2] + 0.25])
    functions["Traj34"] = lambda xt: np.asarray([xt[0][0] - 0.12, xt[0][1], xt[0][2] + 0.25])
    functions["Traj35"] = lambda xt: np.asarray([xt[0][0] + 0.12, xt[0][1] + 0.12, xt[0][2] + 0.25])
    functions["Traj36"] = lambda xt: np.asarray([xt[0][0] + 0.12, xt[0][1] - 0.12, xt[0][2] + 0.25])
    functions["Traj37"] = lambda xt: np.asarray([xt[0][0] - 0.12, xt[0][1] + 0.12, xt[0][2] + 0.25])
    functions["Traj38"] = lambda xt: np.asarray([xt[0][0] - 0.12, xt[0][1] - 0.12, xt[0][2] + 0.25])
    functions["Traj39"] = lambda xt: np.asarray([xt[0][0], xt[0][1] + 0.25, xt[0][2] - 0.25])
    functions["Traj40"] = lambda xt: np.asarray([xt[0][0], xt[0][1] - 0.25, xt[0][2] - 0.25])
    functions["Traj41"] = lambda xt: np.asarray([xt[0][0] + 0.25, xt[0][1], xt[0][2] - 0.25])
    functions["Traj42"] = lambda xt: np.asarray([xt[0][0] - 0.25, xt[0][1], xt[0][2] - 0.25])
    functions["Traj43"] = lambda xt: np.asarray([xt[0][0], xt[0][1] + 0.12, xt[0][2] - 0.25])
    functions["Traj44"] = lambda xt: np.asarray([xt[0][0], xt[0][1] - 0.12, xt[0][2] - 0.25])
    functions["Traj45"] = lambda xt: np.asarray([xt[0][0] + 0.12, xt[0][1], xt[0][2] - 0.25])
    functions["Traj46"] = lambda xt: np.asarray([xt[0][0] - 0.12, xt[0][1], xt[0][2] - 0.25])
    functions["Traj47"] = lambda xt: np.asarray([xt[0][0] + 0.12, xt[0][1] + 0.12, xt[0][2] - 0.25])
    functions["Traj48"] = lambda xt: np.asarray([xt[0][0] + 0.12, xt[0][1] - 0.12, xt[0][2] - 0.25])
    functions["Traj49"] = lambda xt: np.asarray([xt[0][0] - 0.12, xt[0][1] + 0.12, xt[0][2] - 0.25])
    functions["Traj50"] = lambda xt: np.asarray([xt[0][0] - 0.12, xt[0][1] - 0.12, xt[0][2] - 0.25])
    functions["Traj51"] = lambda xt: np.asarray([xt[0][0]-0.5,xt[0][1],xt[0][2]+ 0.25])
    functions["Traj52"] = lambda xt: np.asarray([xt[0][0] - 0.5, xt[0][1], xt[0][2]])
    functions["Traj53"] = lambda xt: np.asarray([xt[0][0]-0.5, xt[0][1], xt[0][2]-0.25])
    functions["Traj54"] = lambda xt: np.asarray([xt[0][0]-0.25, xt[0][1]+ 0.25, xt[0][2]])
    functions["Traj55"] = lambda xt: np.asarray([xt[0][0]-0.25, xt[0][1]-0.25, xt[0][2]])
    functions["Traj56"] = lambda xt: np.asarray([xt[0][0]-0.4, xt[0][1]+0.25, xt[0][2]])
    functions["Traj57"] = lambda xt: np.asarray([xt[0][0]-0.4, xt[0][1]-0.25, xt[0][2]])
    functions["Traj58"] = lambda xt: np.asarray([xt[0][0]-0.12, xt[0][1]+0.12, xt[0][2]])
    functions["Traj59"] = lambda xt: np.asarray([xt[0][0]-0.12, xt[0][1]-0.12, xt[0][2]])
    functions["Traj60"] = lambda xt: np.asarray([xt[0][0]-0.25, xt[0][1]+ 0.25, xt[0][2]+0.12])
    functions["Traj61"] = lambda xt: np.asarray([xt[0][0]-0.25, xt[0][1]-0.25, xt[0][2]+0.12])
    functions["Traj62"] = lambda xt: np.asarray([xt[0][0]-0.4, xt[0][1]+0.25, xt[0][2]+0.12])
    functions["Traj63"] = lambda xt: np.asarray([xt[0][0]-0.4, xt[0][1]-0.25, xt[0][2]+0.12])
    functions["Traj64"] = lambda xt: np.asarray([xt[0][0]-0.12, xt[0][1]+0.12, xt[0][2]+0.12])
    functions["Traj65"] = lambda xt: np.asarray([xt[0][0]-0.12, xt[0][1]-0.12, xt[0][2]+0.12])
    functions["Traj66"] = lambda xt: np.asarray([xt[0][0]-0.25, xt[0][1]+ 0.25, xt[0][2]-0.12])
    functions["Traj67"] = lambda xt: np.asarray([xt[0][0]-0.25, xt[0][1]-0.25, xt[0][2]-0.12])
    functions["Traj68"] = lambda xt: np.asarray([xt[0][0]-0.4, xt[0][1]+0.25, xt[0][2]-0.12])
    functions["Traj69"] = lambda xt: np.asarray([xt[0][0]-0.4, xt[0][1]-0.25, xt[0][2]-0.12])
    functions["Traj70"] = lambda xt: np.asarray([xt[0][0]-0.12, xt[0][1]+0.12, xt[0][2]-0.12])
    functions["Traj71"] = lambda xt: np.asarray([xt[0][0]-0.12, xt[0][1]-0.12, xt[0][2]-0.12])
    functions["Traj72"] = lambda xt: np.asarray([xt[0][0]-0.25, xt[0][1]+ 0.25, xt[0][2]+0.25])
    functions["Traj73"] = lambda xt: np.asarray([xt[0][0]-0.25, xt[0][1]-0.25, xt[0][2]+0.25])
    functions["Traj74"] = lambda xt: np.asarray([xt[0][0]-0.4, xt[0][1]+0.25, xt[0][2]+0.25])
    functions["Traj75"] = lambda xt: np.asarray([xt[0][0]-0.4, xt[0][1]-0.25, xt[0][2]+0.25])
    functions["Traj76"] = lambda xt: np.asarray([xt[0][0]-0.12, xt[0][1]+0.12, xt[0][2]+0.25])
    functions["Traj77"] = lambda xt: np.asarray([xt[0][0]-0.12, xt[0][1]-0.12, xt[0][2]+0.25])
    functions["Traj78"] = lambda xt: np.asarray([xt[0][0]-0.25, xt[0][1]+ 0.25, xt[0][2]-0.25])
    functions["Traj79"] = lambda xt: np.asarray([xt[0][0]-0.25, xt[0][1]-0.25, xt[0][2]-0.25])
    functions["Traj80"] = lambda xt: np.asarray([xt[0][0]-0.4, xt[0][1]+0.25, xt[0][2]-0.25])
    functions["Traj81"] = lambda xt: np.asarray([xt[0][0]-0.4, xt[0][1]-0.25, xt[0][2]-0.25])
    functions["Traj82"] = lambda xt: np.asarray([xt[0][0]-0.12, xt[0][1]+0.12, xt[0][2]-0.25])
    functions["Traj83"] = lambda xt: np.asarray([xt[0][0]-0.12, xt[0][1]-0.12, xt[0][2]-0.25])

def real_args(args,orgpos):
    for x in range(84):
        args["Traj" + str(x)] = [orgpos, 0]

